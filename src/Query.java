/* Interface Query is the Component in the Structural Decorator pattern. The interface contains a single method,
 * send(String message)
 */

public interface Query {
    void send(String message);
}

